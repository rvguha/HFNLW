# Optional: raises Hub rate limits and is required for gated repos you have access to.
HF_TOKEN=

# Enrichment via OpenRouter (config/selection.yaml -> enrichment.backend: openrouter).
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Enrichment via the first-party Anthropic API (enrichment.backend: anthropic).
ANTHROPIC_API_KEY=
